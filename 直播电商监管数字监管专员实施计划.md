# 直播电商监管数字监管专员建设 · 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 依据已批准的《直播电商监管数字监管专员建设方案（v2）》，分阶段落地"监测—识别—固证—分派—处置"人机协同闭环，先在单平台×单品类试点验证，再扩展推广。

**Architecture:** 五层架构（L1 数据层 / L2 执行层双通道 / L3 本体层 / L4 技能层 / L5 应用层）。执行层拆分为"全量监测通道（无头拉流+ASR/OCR+规则/AI 流水线，水平扩展）"与"深度巡检通道（UI-TARS-desktop 复核固证助手，低并发高复杂度）"。数据以平台协查为主通道，自主采集仅作监测与固证补充。

**Tech Stack（建议，立项时确认）：** 流媒体拉流 FFmpeg；ASR 采用本地化中文转写引擎（如 Whisper 系，RTF<0.1，离线部署）；规则引擎自研轻量规则服务（规则本体驱动）；AI 辅助识别采用本地部署多模态模型；UI-TARS-desktop 用于深度巡检；政务内网/专网本地化部署，等保 2.0 三级。

## 范围说明

本计划覆盖方案 v2 的完整实施路径（阶段0—3）。四个阶段是同一系统的顺序里程碑，非独立子系统，故合并在单一计划中；每个阶段末均有独立可验收的交付物，可单独设里程碑评审。

## Global Constraints

- 法规引用一律以官方原文为准：任何规则条款在抓取并核对《直播电商监督管理办法》总局令原文前，不得写入正式规则库。
- 数据合规红线：平台协查为主通道；不绕过登录/验证码采集非公开数据；不采集与监管无关的个人信息；主播声音/人脸仅特征化使用，不留存可还原生物特征数据；全部采集行为入审计日志。
- 处置权归位：系统与 AI 不直接作出任何对外处置；红级线索必须 2 名执法人员复核确认后方可进入处置程序；平台侧动作（下架/封禁/限流）仅通过协查/约谈机制要求平台落实。
- 固证合规：每个固证包必须含 SHA-256 哈希与可信时间戳，取证设备专用且环境清洁。
- 部署合规：核心系统本地化部署于政务内网/专网，数据不出域；不采用公有云 SaaS 承载监管数据。
- 试点先行：阶段1 仅限单平台×单品类（食品或美妆）×试点区县，验收达标前不扩大范围。
- 指标口径：以方案 v2 第十二章定义为准（精确率=系统判定违规且复核属实/系统判定违规总数；召回率=系统发现的属实违规/抽样评估属实违规总数；覆盖率=实际监测/试点范围内可识别直播间总数）。
- 交付物语言：全部正式文档使用简体中文，法规条款原文不得改写。

---

## 阶段0 · 规则基座（1—2 周）

### Task 0.1: 抓取并归档《直播电商监督管理办法》全文

**Files:**
- Create: `docs/regulatory/直播电商监督管理办法全文.md`
- Modify: `网络交易监管资料汇编.md`（补全第 3 项文件链接的全文条目）

**Interfaces:**
- Consumes: 资料汇编中已收录的官方链接（https://www.samr.gov.cn/wljys/gzzd/art/2026/art_fe860c192a4c45e0a77e39c14543be69.html）
- Produces: `法规条款清单（含章节号、条号、原文文本）`，供 Task 0.2 使用

- [ ] **Step 1: 抓取官方页面全文**

通过 ego-browser（或 curl）访问总局网监司规则制度栏目对应页面，抓取《直播电商监督管理办法》全文，保存原文（含发布文号、施行日期）。

- [ ] **Step 2: 校验完整性**

比对页面章节结构，确认"章—条"编号连续、无缺页；记录施行日期、条款总数；将校验结果写入文档头部说明。

- [ ] **Step 3: 结构化条款清单**

按"条号 | 原文 | 主题词"格式输出结构化条款清单，供规则映射使用。

- [ ] **Step 4: 更新资料汇编**

在 `网络交易监管资料汇编.md` 中补充全文链接与要点，保持资料库单一事实源。

- [ ] **Step 5: 验收**

核对：文档含全部章节与条款原文；施行日期与官方一致；资料汇编已更新。验收通过后进入 Task 0.2。

### Task 0.2: 编制规则条款映射表（规则本体初版）

**Files:**
- Create: `docs/regulatory/规则条款映射表.md`
- Create: `config/rules/rules_v0.yaml`（机器可读规则清单，供规则引擎消费）

**Interfaces:**
- Consumes: Task 0.1 的结构化条款清单
- Produces: 规则记录格式 `{rule_id, rule_type, trigger, article_ref, level}`；其中 `rule_type ∈ {禁令, 阈值, 义务}`，`level ∈ {红, 黄}`；供 Task 1.3 规则引擎加载

- [ ] **Step 1: 逐条映射**

按方案 v2 附录 A 框架，将《直播电商监督管理办法》中可机器化的条款逐条映射为规则记录；每条规则必须包含：`rule_id`（R-001 起编号）、`rule_type`、`trigger`（触发条件，如话术关键词、价格波动阈值表达式）、`article_ref`（对应条号）、`level`（红/黄分级）。

- [ ] **Step 2: 阈值规则定参**

对阈值类规则（价格波动、订单金额等），由业务方提供初值并记录到 `rules_v0.yaml`；未定参的阈值规则标记 `status: pending_params`，禁止直接启用。

- [ ] **Step 3: 输出 YAML 规则清单**

将映射表内容同步为 `config/rules/rules_v0.yaml`，格式示例：

```yaml
rules:
  - rule_id: R-001
    rule_type: 禁令
    trigger: "话术包含绝对化用语（全网最好|全国第一|顶级|最先进）"
    article_ref: "第X条"
    level: 红
    status: active
```

- [ ] **Step 4: 验收**

核对：每条规则均有 `article_ref` 且与 Task 0.1 条款清单一致；无 `status: pending_params` 的规则处于 `active`；映射表覆盖方案 v2 附录 A 的 R-001～R-004 对应条款。验收通过后进入 Task 0.3。

### Task 0.3: 搭建金标准评测集框架

**Files:**
- Create: `docs/regulatory/评测集标注规范.md`
- Create: `data/golden_set/golden_v0.jsonl`（首批人工标注样本，≥100 条）

**Interfaces:**
- Consumes: Task 0.2 规则清单（作为标注时的判定依据）
- Produces: 标注样本格式 `{sample_id, text, is_violation, violation_type, rule_ids[], labeler, agreed}`，供 Task 1.3 评测

- [ ] **Step 1: 编写标注规范**

定义：样本来源（试点品类的真实直播话术/商品描述，脱敏后使用）、判定标准（对照规则清单）、标注字段、争议处理（第三人仲裁）。

- [ ] **Step 2: 首批标注**

由 2 名业务人员独立标注 ≥100 条样本（正负例比例约 1:2）；标注结果写入 `golden_v0.jsonl`，每条约含 `sample_id / text / is_violation / violation_type / rule_ids / labeler`。

- [ ] **Step 3: 一致性校验**

比对双人标注，计算一致率；不一致样本由第三人仲裁并标记 `agreed: true`。

- [ ] **Step 4: 验收**

核对：标注样本 ≥100 条；双人标注一致率 ≥90%（否则补充仲裁后重测）；`golden_v0.jsonl` 结构完整。验收通过后进入 Task 1.3 依赖就绪。

---

## 阶段1 · 试点闭环（3 个月，单平台×单品类×试点区县）

### Task 1.1: 数据接入（协查数据 + 公开直播列表）

**Files:**
- Create: `docs/regulatory/数据接入与协查接口说明.md`
- Create: `config/sources/pilot_platform.yaml`（试点平台、品类、区县配置）

**Interfaces:**
- Consumes: 试点平台协查数据（依据《网络交易执法协查暂行办法》）；公开直播间列表
- Produces: 直播间清单数据流 `live_rooms[]`（字段：`room_id / platform / category / merchant_id / start_time / status`），供 Task 1.2 拉流；产出文档供 Task 1.5 分派属地规则使用

- [ ] **Step 1: 建立协查通道**

与试点平台属地监管部门/平台方协商数据对接方式（接口/文件传输），明确字段范围与频次，形成《数据接入与协查接口说明.md》。

- [ ] **Step 2: 试点配置**

在 `pilot_platform.yaml` 中写入试点平台、目标品类（食品或美妆）、试点区县范围；配置直播间列表同步任务（按日更新）。

- [ ] **Step 3: 采集与落库**

实现公开直播间列表采集（仅公开展示页），与协查数据合并去重后写入直播间清单库。

- [ ] **Step 4: 验收**

核对：试点范围内可识别直播间列表日更新完整率 ≥95%（连续 7 日统计）；采集行为全部入审计日志；无绕过登录/验证码行为。

### Task 1.2: 流媒体拉流与 ASR 转写流水线

**Files:**
- Create: `pipelines/stream_ingest/`（拉流、转写、抽帧三个模块的工程目录与运行文档）
- Create: `docs/engineering/拉流转写流水线运行手册.md`

**Interfaces:**
- Consumes: Task 1.1 的 `live_rooms[]`；试点平台公开直播流地址
- Produces: 转写文本流 `transcript{room_id, ts_start, ts_end, text, asr_conf}` 与关键帧 OCR 结果 `frame_ocr{room_id, ts, text, image_ref}`，供 Task 1.3 识别；供 Task 1.4 固证包引用

- [ ] **Step 1: 拉流模块**

基于 FFmpeg 实现多路并发拉流（按试点直播间清单动态启停），断流自动重连，单路超时降级为轮询重试；记录拉流日志（起止时间、丢流率）。

- [ ] **Step 2: ASR 转写模块**

部署本地化中文转写引擎，实现音频实时转写（RTF<0.1）与关键帧抽帧 OCR；转写结果带时间戳区间与置信度。

- [ ] **Step 3: 72 小时试运行**

对试点平台目标品类的全部直播间连续运行 72 小时，统计丢流率、转写 RTF、OCR 抽帧完整率。

- [ ] **Step 4: 验收**

核对：丢流率 <1%；单路转写 RTF<0.1；抽查 10 段转写文本，与人工听写比对，字准率 ≥85%；试运行报告写入运行手册。

### Task 1.3: 规则引擎 + 双模型识别

**Files:**
- Create: `services/rule_engine/`（规则引擎服务，加载 `rules_v0.yaml`）
- Create: `services/ai_detect/`（AI 辅助识别服务，输出置信度）
- Create: `docs/engineering/识别服务评测报告.md`

**Interfaces:**
- Consumes: Task 1.2 的 `transcript[]`、`frame_ocr[]`；Task 0.2 的 `rules_v0.yaml`；Task 0.3 的 `golden_v0.jsonl`
- Produces: 线索对象 `clue{clue_id, room_id, level(红|黄|绿), rule_ids[], ai_conf, evidence_refs[]}`，供 Task 1.4 固证、Task 1.5 分派

- [ ] **Step 1: 规则引擎实现**

实现规则加载与匹配：规则命中 → 生成红级线索；规则未命中但 AI 判定违规且置信度 ≥0.8 → 生成黄级线索；其余为绿级归档。

- [ ] **Step 2: AI 识别集成**

部署本地多模态模型对转写文本与 OCR 结果做违规判定，输出 `ai_conf ∈ [0,1]`；置信度低于 0.8 不直接产生黄级线索。

- [ ] **Step 3: 金标准评测**

在 `golden_v0.jsonl` 上评测双通道（规则通道 + AI 通道）联合效果，计算精确率、召回率、F1；输出评测报告。

- [ ] **Step 4: 验收**

核对：试点期目标——精确率 ≥85%、召回率 ≥80%、F1 ≥0.82；红级线索全部由规则命中产生（可回溯 `rule_ids`）；评测报告留存。

### Task 1.4: 固证包生成与存证

**Files:**
- Create: `services/evidence/`（固证包生成服务）
- Create: `docs/engineering/固证与存证说明.md`

**Interfaces:**
- Consumes: Task 1.3 的 `clue[]`；Task 1.2 的原始片段、转写、OCR、页面快照
- Produces: 固证包 `evidence_package{package_id, clue_id, video_ref, transcript_ref, snapshot_refs[], meta{capture_time, device_id, source_url, room_id}, sha256, timestamp_token}`，供 Task 1.5 分派、Task 1.6 处置工作台查看

- [ ] **Step 1: 固证包组装**

对每条红/黄线索，自动组装：违规片段（上下文前后各 30 秒）+ 转写文本 + 页面快照 + 取证元数据（NTP 校时采集时间、设备标识、源 URL、直播场次标识）。

- [ ] **Step 2: 哈希与时间戳**

对固证包计算 SHA-256 哈希，接入可信时间戳服务（或区块链存证），生成 `timestamp_token` 并随包存储。

- [ ] **Step 3: 存储隔离**

原始采集数据与固证包分离存储；固证包目录只读，任何修改触发哈希校验告警。

- [ ] **Step 4: 验收**

核对：抽查 10 个固证包，哈希可复算且与记录一致；时间戳可验证；片段含前后 30 秒上下文；固证完整率 100%。

### Task 1.5: 线索库与自动分派

**Files:**
- Create: `services/dispatch/`（线索库与分派服务）
- Create: `docs/engineering/分派规则说明.md`

**Interfaces:**
- Consumes: Task 1.4 的 `evidence_package[]`；Task 1.1 的属地配置
- Produces: 分派单 `dispatch{dispatch_id, clue_id, org_code, status(待复核|复核中|已处置|已结案), assigned_at}`，供 Task 1.6 工作台消费

- [ ] **Step 1: 线索库实现**

实现线索/固证包/分派单的数据模型与查询接口，记录全量决策日志（识别→固证→分派各环节时间戳与操作者）。

- [ ] **Step 2: 属地分派规则**

按试点区县属地规则自动分派至基层执法账号；红级线索强制进入"待复核"状态并通知 2 名执法人员。

- [ ] **Step 3: 时效监控**

统计"线索生成 → 分派"时长，超过 10 分钟自动告警。

- [ ] **Step 4: 验收**

核对：线索生成→分派 ≤10 分钟（连续 7 日统计达标）；红级线索全部进入双人复核流程；分派日志完整可审计。

### Task 1.6: 处置工作台（人工复核与执法台账）

**Files:**
- Create: `apps/dispatch_console/`（处置工作台前端与后端）
- Create: `docs/engineering/处置工作台用户手册.md`

**Interfaces:**
- Consumes: Task 1.5 的 `dispatch[]`、`evidence_package[]`
- Produces: 处置记录 `action{action_id, dispatch_id, action_type(监管侧|平台协同侧), action_content, reviewer1, reviewer2, status, closed_at}`；供 Task 1.8 试点评估统计

- [ ] **Step 1: 复核流程实现**

实现红级双人复核（两人独立确认/驳回）、黄级初筛（确认违规则升级）、绿级归档；复核操作全部留痕。

- [ ] **Step 2: 处置建议书生成**

复核确认后自动生成处置建议书（关联线索、固证包、规则条款、建议的监管侧/平台协同侧动作），由执法人员编辑后走法定程序。

- [ ] **Step 3: 执法台账**

记录预警、责令改正、约谈、立案、信用公示等监管侧动作及平台协同侧动作的发起与反馈跟踪。

- [ ] **Step 4: 验收**

核对：红级线索 100% 走双人复核；处置动作均由人工发起，系统无自动对外处置出口；台账与日志完整。

### Task 1.7: 监管大屏与合规服务

**Files:**
- Create: `apps/monitor_dashboard/`（监管大屏）
- Create: `services/compliance_guide/`（合规提示服务）

**Interfaces:**
- Consumes: Task 1.5 线索库统计数据；Task 1.6 处置记录
- Produces: 大屏指标（覆盖率、线索分布、处置进度，口径见 Global Constraints）；合规提示书 `compliance_guide{guide_id, merchant_id, content, sent_at}`

- [ ] **Step 1: 大屏指标**

按方案 v2 第十二章指标口径实现大屏统计（覆盖率、红黄绿线索分布、处置进度、复核率）。

- [ ] **Step 2: 合规提示**

对轻微/首次问题生成合规提示或行政指导书草稿，经执法人员确认后发送。

- [ ] **Step 3: 验收**

核对：大屏指标与口径定义一致（抽样比对 10 项数值）；合规提示发送须经人工确认。

### Task 1.8: 试点运行与评估（3 个月）

**Files:**
- Create: `docs/regulatory/试点评估报告.md`

**Interfaces:**
- Consumes: Task 1.3 评测报告、Task 1.6 处置记录、Task 1.7 大屏统计
- Produces: 阶段1 验收结论（达标/不达标及改进项），作为进入阶段2 的闸门

- [ ] **Step 1: 连续运行与数据积累**

试点平台×目标品类连续运行 3 个月，按月输出指标（精确率/召回率/F1/覆盖率/复核率/误判申诉率）。

- [ ] **Step 2: 评测集扩容与成本实测**

将试点期真实线索并入金标准评测集（双人标注），目标累计 ≥500 条；同步统计 GPU/带宽/存储实际用量，修正方案 v2 第十四章量级估算，输出成本修正表。

- [ ] **Step 3: 试点评估报告**

汇总指标达成情况、问题清单（如误判类型、拉流稳定性、分派超时）、改进建议；对照方案 v2 第十二章试点期目标逐项给结论。

- [ ] **Step 4: 验收**

核对：试点期指标达成（精确率 ≥85%、召回率 ≥80%、F1 ≥0.82、覆盖率 ≥80%、复核率 ≤20%、误判申诉率 ≤1%、固证完整率 100%）；未达标项有明确改进计划后方可进入阶段2。

---

## 阶段2 · 双通道扩展（2—3 个月）

### Task 2.1: 深度巡检通道（UI-TARS-desktop 机群）

**Files:**
- Create: `services/deep_inspect/`（任务队列 + 巡检机调度）
- Create: `docs/engineering/深度巡检通道运行手册.md`

**Interfaces:**
- Consumes: Task 1.5 的黄级待初筛线索、专项检查任务、执法人员下钻指令
- Produces: 巡检结果 `inspect_result{task_id, room_id, actions[], snapshots[], video_refs[], status, timeout_flag}`，回写线索库与证据库

- [ ] **Step 1: 巡检机群部署**

在专用巡检机上部署 UI-TARS-desktop（单机单会话），配置白名单站点（试点平台），接入任务队列；巡检机与政务内网隔离，账号与网络路径审计。

- [ ] **Step 2: 任务调度**

实现任务队列：黄级线索复核、专项检查、下钻指令入队，多机并行消费；单任务超时自动转人工处理。

- [ ] **Step 3: 页面级固证**

实现打开直播间/店铺页面 → 视觉理解 → 查看资质/历史记录/挂载商品 → 录屏/截图 → 结果回写。

- [ ] **Step 4: 验收**

核对：任务队列可并发调度（≥2 台机并行）；超时任务 100% 转人工；巡检操作全部留痕；白名单外站点不可访问。

### Task 2.2: 扩品类、扩平台

**Files:**
- Modify: `config/sources/pilot_platform.yaml`（新增平台/品类配置）
- Create: `docs/regulatory/平台接入记录.md`

**Interfaces:**
- Consumes: Task 1.1 数据接入通道、Task 1.2 拉流转写流水线（复用）
- Produces: 新平台直播间清单数据流（复用 `live_rooms[]` 结构）

- [ ] **Step 1: 平台接入**

按 Task 1.1 流程接入新平台（协查对接 + 公开列表采集），登记到接入记录。

- [ ] **Step 2: 品类扩展**

按试点品类验证流程扩展到新品类（配置规则阈值与标注样本扩展）。

- [ ] **Step 3: 验收**

核对：新平台接入周期 ≤2 周/个；新品类上线前完成对应金标准样本标注（≥50 条）；扩展后覆盖率目标 ≥85%。

### Task 2.3: 安全加固与对抗防护

**Files:**
- Create: `docs/engineering/安全设计与等保测评报告.md`
- Create: `config/security/adversarial_samples_v0.jsonl`（对抗样本集）

**Interfaces:**
- Consumes: Task 1.3 识别服务、Task 1.4 固证服务
- Produces: 安全基线配置；对抗样本评测结论（用于识别服务阈值/规则调整）

- [ ] **Step 1: 对抗样本集**

构造对抗样本（诱导误判的话术/画面，如"本产品全网最……（刻意断句）"），≥50 条入评测集。

- [ ] **Step 2: 防护验证**

验证规则优先、置信度阈值、红级人工复核对对抗样本的拦截效果；必要时调整规则触发与阈值。

- [ ] **Step 3: 等保测评**

按等保 2.0 三级基线自查并形成测评报告（身份鉴别、访问控制、审计、加密、备份）。

- [ ] **Step 4: 验收**

核对：对抗样本误判率 ≤5%；等保三级自查项全部落实；供应链（模型/组件）许可与哈希校验记录完整。

---

## 阶段3 · 推广与联动（持续推进）

### Task 3.1: 全市/全省推广部署

**Files:**
- Create: `docs/deployment/部署与培训手册.md`
- Create: `docs/deployment/推广部署记录.md`

**Interfaces:**
- Consumes: 阶段2 通过验收的系统与配置
- Produces: 推广部署实例（省级统建、市县终端接入）

- [ ] **Step 1: 部署手册**

编写部署手册（环境要求、安装步骤、初始配置、故障处理），覆盖省级统建/市县接入两种模式。

- [ ] **Step 2: 培训与上线**

对市县执法人员培训（复核流程、工作台操作、大屏解读），按区域分批上线。

- [ ] **Step 3: 验收**

核对：推广范围内试点期指标复现（精确率 ≥90%、召回率 ≥85%、覆盖率 ≥95%）；培训覆盖率 100% 记录在案。

### Task 3.2: 信用监管与 12315 联动

**Files:**
- Create: `services/credit_link/`（信用归集接口）
- Create: `services/consumer_complaint_link/`（12315 线索对接）

**Interfaces:**
- Consumes: Task 1.6 处置结果；12315 平台投诉数据（内部对接）
- Produces: 信用归集数据（按《网络交易监督管理办法》第37条口径）、投诉关联线索

- [ ] **Step 1: 信用联动**

将已处置案件信息按第37条口径归集至国家企业信用信息公示系统对接通道。

- [ ] **Step 2: 12315 联动**

接入 12315 投诉数据，投诉对象与直播间/经营者自动关联，作为线索补充来源（投诉人信息脱敏）。

- [ ] **Step 3: 验收**

核对：信用归集链路可验证（抽样比对 5 条）；投诉关联线索可正常进入分派流程。

### Task 3.3: 常态化运营机制

**Files:**
- Create: `docs/operations/运营制度与报告模板.md`

**Interfaces:**
- Consumes: 全系统运行数据
- Produces: 周报/月报模板、金标准标注小组工作指引、规则更新回归流程

- [ ] **Step 1: 制度文件**

制定周报/月报机制、标注小组工作指引、法规更新时规则本体更新与回归评测流程。

- [ ] **Step 2: 验收**

核对：制度文件齐备；规则更新回归流程明确（法规更新 → 规则修订 → 金标准回归 → 上线回滚预案）。

---

## 自审记录

**规格覆盖核对**：方案 v2 第〇章待核实项→Task 0.1/0.3/2.1 与平台接入记录；第一～二章→阶段1 整体；第三章数据合规矩阵→Task 1.1/2.2；第四章本体→Task 0.2/0.3；第五章技能体系→Task 1.2/1.3/2.1（SKILL_001-008 对应流水线各模块）；第六章双通道→Task 1.2/2.1；第七章证据链→Task 1.4；第八章处置权与救济→Task 1.5/1.6；第九章应用层→Task 1.7；第十章部署与安全→Task 2.3/3.1；第十一章运营→Task 1.8/3.3；第十二章指标→各 Task 验收标准；第十三章实施路径→阶段0-3；第十四章成本→Task 1.8 Step 2 成本实测；第十五章风险→Task 0.1/2.3 及验收闸门。

**占位检查**：规则条款以 Task 0.1 抓取原文为准，映射表在原文核对前不启用（Task 0.2 已设 `status: pending_params` 闸门）；无 "TBD/TODO" 式空步骤；每 Task 均有可执行步骤与验收标准。

**一致性检查**：线索/固证包/分派单/处置记录等跨 Task 数据结构字段在 Interfaces 中统一定义，后续 Task 引用同一字段名（`clue_id`、`evidence_package`、`dispatch_id`、`room_id` 等）；指标口径引用方案 v2 第十二章定义。
