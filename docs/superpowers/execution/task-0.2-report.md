# Task 0.2 执行报告：编制规则条款映射表（规则本体初版）

- 状态：DONE
- 执行时间：2026-08-07
- 任务阶段：直播电商监管数字监管专员项目 Phase 0 / Task 0.2

## 一、执行内容

1. **Step 1（逐条映射）**：阅读任务简报与 `docs/regulatory/直播电商监督管理办法全文.md` 的「二、条款清单」及「三、全文」（重点逐条核对第八、九、十一～二十、二十五～四十五、四十六～五十二条原文），按方案 v2 附录 A 框架将可机器化条款映射为规则记录，输出 `docs/regulatory/规则条款映射表.md`（含说明、R-001～R-004 基线映射标注、逐条映射表、统计）。
2. **Step 2（阈值规则定参）**：阈值类规则（重点对象规模 R-006、投诉集中度 R-014、促销折扣幅度 R-028、价格波动幅度 R-029）业务方尚未提供初值，全部标记 `status: pending_params`，禁止直接启用；触发条件中保留阈值表达式占位（`≥X%`、`近X日`等）。法定固定值（三个月/六个月/三年、三个/七个工作日、一月/七月报送等）不视为待定参数，对应规则置 `active`。
3. **Step 3（输出 YAML）**：生成 `config/rules/rules_v0.yaml`，与映射表同 ID、同字段、同口径。
4. **Step 4（验收）**：见下节校验结果，验收通过。

## 二、校验结果

### 规则统计（35 条）

| 维度 | 统计 |
| --- | --- |
| 按 rule_type | 禁令 8（R-001、R-002、R-019、R-023、R-024、R-025、R-027、R-034）；阈值 2（R-028、R-029）；义务 25 |
| 按 level | 红 13；黄 22 |
| 按 status | active 31；pending_params 4（R-006、R-014、R-028、R-029） |
| 合计 | 35（R-001—R-035） |

### R-001～R-004 核实结果（基线覆盖）

| rule_id | 附录 A 主题 | 核实后 article_ref | 依据 |
| --- | --- | --- | --- |
| R-001 | 绝对化用语禁令 | 第三十四条 | 第三十四条第一款禁止对性能、功能、质量、销售情况等作虚假或引人误解的商业宣传 |
| R-002 | 虚假功效/成分禁令 | 第三十四条 | 同上（性能、功能、质量类） |
| R-003 | 平台核验主播身份义务 | 第八条 | 第八条第一款/第三款：核验登记、至少每六个月核验更新、不得为身份不符人员提供服务 |
| R-004 | AI 生成内容标识义务 | 第三十七条 | 第三十七条第一款：AI 生成人物图像、视频应依国家规定标识并持续提示 |

说明：R-001 附录 A 初版曾指向《广告法》，核实后以本办法第三十四条为本规则 `article_ref`；第三十五条（构成商业广告时依广告法履责）在处置环节可并行适用，映射表中已标注，不重复设号。

### YAML 校验（命令与结果）

命令（Python 3 + PyYAML）：

```python
import yaml
data = yaml.safe_load(open('config/rules/rules_v0.yaml', encoding='utf-8'))
```

结果：

- `yaml.safe_load` 解析成功，共 35 条规则（Valid YAML）。
- `article_ref` 全部存在于 Task 0.1 条款清单（69 条，无缺号）；非法条号为 0。
- `rule_type ∈ {禁令, 阈值, 义务}`、`level ∈ {红, 黄}`、`status ∈ {active, pending_params}` 全部合法。
- `pending_params` 规则（R-006、R-014、R-028、R-029）均非 active；其余 31 条全部为 active。
- 映射表主表 35 行与 YAML 35 条规则 ID 集合完全一致（映射表「二、基线映射标注」中的 R-001～R-004 为重复标注行，不计入主表）。

## 三、交付物与文件变更

### 新建文件

- `docs/regulatory/规则条款映射表.md`：说明（映射依据、适用范围、分级口径、状态口径、未纳入条款清单）、R-001～R-004 基线映射结果标注、35 条逐条映射表（每条含 rule_id / rule_type / trigger / article_ref / level / status 及对应条文原文关键句摘录）、统计表。
- `config/rules/rules_v0.yaml`：机器可读规则清单，与映射表同 ID 同字段，供 Task 1.3 规则引擎加载。
- `docs/superpowers/execution/task-0.2-report.md`：本报告。

## 四、注意事项 / Concerns

- 阈值参数（R-006 重点对象规模、R-014 投诉集中度、R-028 折扣幅度、R-029 价格波动幅度）依赖业务方定参，定参后需将对应规则由 `pending_params` 置为 `active` 并回填 trigger 中的 `X` 值。
- 分级口径为 v0 主观设定（红=直接危害消费者权益/严重违法需人工复核，黄=需关注/提示），建议 Task 1.3 规则引擎联调时结合实际复核率校准。
- 触发词（如 R-001/R-002/R-024 中的正则关键词、R-034 刷单行为信号）为初版种子词表，需在引擎落地时按品类扩充并做误报率评估。
- 未纳入 v0 的条款（第十条、第十五条、第二十一～二十三条、第四十一、四十二条及第五章、第六章）已在映射表「一、说明 5」透明记录原因，供 v1 迭代。

## 五、评审修订记录（MD↔YAML 同步修复）

- 日期：2026-08-07
- 问题：评审发现 `config/rules/rules_v0.yaml` 两处 trigger 与 `docs/regulatory/规则条款映射表.md` 不同步。
- 修复内容：
  1. R-024：YAML trigger 补充 `销量第一` 关键词，与映射表一致（现为 `销量破亿|销量第一|好评率100%|央视推荐|XX认证`）。
  2. R-023：YAML trigger 补充实现提示后缀「（违禁类目关键词/资质缺失信号）」，与映射表一致。
- 其余字段与规则未作任何改动。
- 校验命令与结果：

```bash
python3 -c "import yaml; d=yaml.safe_load(open('config/rules/rules_v0.yaml')); print('YAML OK:', len(d['rules']), 'rules')"
```

输出：`YAML OK: 35 rules`（Valid YAML，35 条规则解析成功）。
